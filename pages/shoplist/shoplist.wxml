<!-- 轮播图 -->
<view class="diy-banner">
  <swiper class="banner-box swiper-box dot-{{ itemStyle.btnShape }}" autoplay="{{ autoplay }}" circular="{{ true }}" duration="{{ duration }}"style="height: {{ imgHeights[imgCurrent] }}rpx" indicator-color="#fff" indicator-active-color="#000" indicator-dots="ture">
  <swiper-item wx:for="{{imgList}}">
    <image catchtap="navigationTo" class="slide-image" src="{{item.file_path}}">
    </image>
  </swiper-item>
  </swiper>
</view>
<!-- 分类tab -->
<view class="header">
  <scroll-view scroll-x scroll-left style="height: 100%;">
    <view class="cs {{ category_id ==  0 ? 'active' : '' }}" catchtap="onSwitchTab" data-id="0">
      <text>全部</text>
    </view>
    <!-- 分类列表 -->
    <view class="cs {{  item.category_id == category_id ? 'active' : '' }}" catchtap="onSwitchTab" wx:for="{{ categoryList }}" wx:key="this" data-id="{{ item.category_id }}">
      <text>{{ item.name }}</text>
    </view>
  </scroll-view>
</view>
<!-- 店铺 -->
<view class="diy-shop" style="margin-top:100rpx;">
  <view class="shop-item" wx:for="{{ shopList }}" wx:for-item="dataItem" wx:key="this">
    <form bindsubmit="_onTargetDetail" report-submit="true">
      <button formType="submit" class="btn-normal dis-flex flex-y-center" data-id="{{ dataItem.shop_id }}">
        <view class="shop-item__logo">
          <image src="{{ dataItem.logo.file_path }}"></image>
        </view>
        <view class="shop-item__content flex-box">
          <view class="shop-item__title">
            <span>{{ dataItem.shop_name }}</span>
          </view>
          <view class="shop-item__address onelist-hidden">
            <span>门店地址：{{ dataItem.region.province }}{{ dataItem.region.city }}{{ dataItem.region.region }}{{ dataItem.address }}</span>
          </view>
          <view class="shop-item__phone">
            <span>联系电话：{{ dataItem.phone }}</span>
          </view>
          <view class="shop-item__distance">
            <block wx:if="{{ dataItem.distance }}">
              <text class="iconfont icon-dingwei"></text>
              <text class="f-24">{{ dataItem.distance_unit }}</text>
            </block>
          </view>
        </view>
      </button>
    </form>
  </view>
  <!-- 无数据提供的页面 -->
  <view wx:if="{{ !shopList.length }}">
    <view class="yoshop-notcont">
      <text class="iconfont icon-wushuju"></text>
      <text class="cont">亲，暂无店铺哦</text>
    </view>
  </view>
  <!-- 定位按钮 -->
  <view wx:if="{{ !isAuthor }}" class="widget-location dis-flex flex-x-center flex-y-center" catchtap="onAuthorize">
    <text class="iconfont icon-locate"></text>
  </view>
</view>
<zan-toptips id="zan-toptips" content="{{ content }}" />